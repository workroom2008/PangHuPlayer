# 工程工作流契约（AGENTS.md）

本文件是 coding agent 在本仓库工作时必须遵守的工程工作流。理念源自
[Superpowers](https://github.com/obra/superpowers)：**计划先行、测试先行（TDD）、
用证据说话**。

优先级：用户口头指令 > 本文件 > 默认行为。用户明确要求跳过某条规则时，可以跳过，
但要说明。

---

## 0. 总则

1. **计划先行**：实现前先写计划（多任务时存 `docs/plans/`），动手前先对齐目标。
2. **测试先行（TDD）**：先写失败测试 → 亲眼看它失败 → 最小实现 → 亲眼看它通过 → 重构。
3. **证据优先于声称**：不口头宣布「应该没问题」，跑测试、看输出，用结果说话。
4. **频繁小提交**：每个任务一个提交，提交信息用约定式（见 §5）。
5. **最少改动**：只改实现需求所需的最小代码，YAGNI、DRY。

---

## 1. 测试运行须知（重要）

**本机（Windows）环境配置了 HTTP 代理，会让 `flutter test` 加载测试套件时
连接本地 127.0.0.1 失败**（报 `Connection closed before full header was received`）。
运行测试前必须临时清掉代理变量：

```bash
HTTP_PROXY= HTTPS_PROXY= ALL_PROXY= NO_PROXY=127.0.0.1,localhost flutter test
```

同样适用于 `flutter analyze` 需要访问网络时。若输出被截断想看全量，用
`2>&1 | tail -N`。

---

## 2. 计划先行

- 任务涉及多个文件或非平凡改动时，**先写实施计划再动代码**。
- 计划存放：`docs/plans/YYYY-MM-DD-<功能名>.md`，使用模板
  `docs/plans/_TEMPLATE.md`。
- 计划要写到「零上下文的新工程师也能照做」的程度：
  - 每个任务给出精确文件路径、接口签名、完整代码、验证命令；
  - 禁止占位符：「TBD」「handle edge cases」「Similar to Task N」都不合格；
  - 步骤粒度 2–5 分钟：写失败测试 → 跑（确认失败）→ 实现 → 跑（确认通过）→ 提交。
- 写完计划后自查：spec 是否每条都有任务覆盖？占位符是否清空？前后任务类型/签名是否一致？

---

## 3. TDD（RED-GREEN-REFACTOR）

**铁律：没有先失败的测试，就不写生产代码。** 先写了？删掉重来。

- **RED**：写一个最小失败测试，测**一个**行为，名字描述行为，用真实代码（少 mock）。
- **Verify RED**（强制）：跑测试，确认它**失败**、失败原因符合预期（功能缺失而非拼写错误）。
  测试直接通过 = 测的是已存在的行为，重写测试。
- **GREEN**：写恰好够让测试通过的最小实现。不加测试没要求的功能。
- **Verify GREEN**（强制）：跑测试，确认通过、其他测试仍通过、输出干净。
- **REFACTOR**：全绿后才重构（删重复、改命名、提函数），重构后保持全绿。

完成一个功能前对照：
- [ ] 每个新函数/方法都有测试
- [ ] 每个测试都在实现前亲眼看过它失败
- [ ] 失败原因是功能缺失，不是笔误
- [ ] 用了最小实现
- [ ] 全部测试通过、输出干净

**修 bug 同理**：先写一个能复现该 bug 的失败测试，再修代码。

---

## 4. 验证证据（完成前必做）

任何改动声明「完成」之前，必须：

```bash
HTTP_PROXY= HTTPS_PROXY= ALL_PROXY= NO_PROXY=127.0.0.1,localhost flutter test   # 全绿
flutter analyze                                                                # 无 error（info 级 lint 可与存量一致）
```

- 把测试结果当作交付物的一部分：改动引入新测试时，报告里写明「X 个测试通过」。
- 失败不是终点：先复现、再定位根因、再修，最后用测试证明修好了。
- 存量问题（如本项目遗留的 info 级 lint）不算阻塞，但新改动不应引入新的 error。

---

## 5. 提交规范

- 约定式提交 + 中文描述：`feat:` / `fix:` / `refactor:` / `docs:` / `test:` / `chore:`。
- 正文说明「为什么」而不是「改了什么」。
- 示例：`fix: 修复健康检查定时器在 dispose 后仍触发的问题`

---

## 6. 日常遵守速查（给人类搭档）

每次让我（agent）干活时，你可以直接要求走完整流程：

1. 「**先 brainstorm**」→ 我提问澄清需求，产出设计要点，你确认后再动手。
2. 「**先写计划**」→ 我产出 `docs/plans/` 下的实施计划，你批准后执行。
3. 「**用 TDD 做**」→ 我严格走红绿循环，每个测试先失败再通过。
4. 「**完成后**」→ 我默认跑测试 + analyze 并把证据写进总结。

一句话：**你可以随时打断流程，但默认我会按这个工作流执行。**

---

## 7. Superpowers 技能库（全局）

本机已全局安装 Superpowers 技能库（`~/.freebuff/superpowers/`，含 14 个技能），
全局引导见 `~/.freebuff/AGENTS.md` / `~/AGENTS.md`。涉及本仓库任务时，
技能触发规则同样适用：动手前先检查是否有适用技能，有则先读对应 `SKILL.md` 再执行
（本文件的流程已与 brainstorming / writing-plans / test-driven-development /
verification-before-completion 等技能对齐）。
